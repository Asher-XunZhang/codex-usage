# Changelog

## 1.0.2 — 2026-09-13

本次首次公开发布 Windows x64 便携版。仅提供 Windows 安装包；macOS Apple Silicon / Intel 下载继续为 v1.0.1，新增 Windows 特性将另行同步。本地开发阶段使用过的 1.1–1.4 预览编号不作为公开版本。

### 2026-09-13 Windows 修订

- 修复先运行 Codex 任务、之后才启动本工具时，任务可能呈灰色且提示无法确认开始记录的问题：创建时间带毫秒、开始时间仅为整秒时，旧版会忽略已读到的首次开始记录。
- 支持从已有日志确认正在执行的首轮任务，包含长日志回溯；重新核对旧版留下的错误检查点，无需重新执行任务。保留轮次切换、完成去重及真实缺失记录的待确认行为。
- Windows 发布包及 SHA-256 文件以修订产物替换，版本号继续为 v1.0.2；此前下载的用户需重新下载完整 ZIP。macOS v1.0.1 安装包不变。

### Windows 原生桌面与预算提醒

- 新增 C# / WPF 主面板、Win32 托盘、圆形浮窗，使用共享 Python 解析与 SQLite 索引；不使用 WebView。关闭主面板释放窗口及统计服务，常驻入口继续运行。
- 支持时间、模型、任务筛选，趋势、搜索、排序与 CSV 导出；筛选与结果快照绑定，异步失败可重试，导出对应已确认的显示快照。
- Token、估算金额、官方余量下限预算支持独立范围、周期、阈值、暂停与去重；日期错误保留输入，草稿独立恢复，并发修改保留恢复路径，缺价与失效额度窗口提供解决入口。
- 本地日志与账号额度独立显示更新时间、错误和重试；统一主题与设置入口，托盘模式也可选择外观。

### 任务监控

- 独立选择正在执行的任务，设置本轮或每轮结束后提醒；使用明确生命周期事件，不把日志安静、子任务结束或断线判定为主任务完成。
- 支持长任务有界回溯、订阅与消息持久化、重启去重、离线补记和来源故障恢复。无法确认的状态明确标记；来源不支持的审批或失败分类不会猜测。
- 圆环、四向侧签与托盘持续显示监控符号；浮窗独立监控页包含状态摘要、未读消息、取消单项监控、清理已结束项和连接检查反馈。
- 提供无声系统通知与应用内标记，支持仅应用内、暂停及全屏抑制；不抢焦点、不自动切页，恢复通知不逐条补弹历史消息。系统横幅呈现与真实点击链路的验收边界见 Windows 验证文档。

### 浮窗交互与可访问性

- 圆环与展开栏使用单一轮廓连续形变，支持中途反向，避免残影与内容裁切；遵循系统动画设置。
- 浮窗可在展开后的整个可见区域拖动，超过阈值后不再触发原按钮；拖动过程自由跟随，松手后恢复越界位置，下一次展开按新位置重新选择方向。
- 四边自动隐藏为额度侧签，避开相邻屏幕连通接缝；收起圆环可直接抓取，展开过渡不抢走拖动，修复鼠标离开后偶发不能收起。
- 统一分段、菜单、下拉及日历样式，修正表头对齐、搜索圆角、按钮字形居中与键盘焦点；“保持展开”、置顶、隐藏和退出含义独立。

### 工程、兼容与发行

- 源码按 src/backend、src/macos、src/windows 分层；工具、测试、资源与文档按平台整理，旧命令保留兼容入口。
- Windows 父进程监测使用进程退出与停止事件，消除周期超时；macOS 主面板维持立即首检及 0.5 秒间隔，短时采集保持先等待 0.2 秒。
- Windows 后端退出使用每次启动生成的私密凭据，公开 /health 实例标识不能授权退出；macOS 不开放该退出接口，主面板启动校验保持原基线。
- 自包含 .NET / Python、固定下载校验、完整第三方许可、两端可重建源码清单与离线发行验证；不随包发布个人日志、配置、数据库或本地排查记录。
- 同步双平台下载入口、预算/监控指南、架构和验证边界；macOS 安装助手继续固定 v1.0.1。

## 1.0.1 — 2026-09-11

- 收起浮窗的剩余额度弧线随额度降低，从绿经过黄、橙连续转为红色，深浅主题分别适配配色。
- 5% 及以下保持警示红；0% 只保留中性底环，未知额度显示「—」。颜色复用现有额度动画，不增加常驻计时器。
- 更新浮窗示意图、配色说明与验证记录。新配色包含在本版 Apple Silicon / Intel 发行包中。
- 发布独立的安装助手及 SHA-256 文件，固定校验本版两个架构的安装包。
- 新增 GitHub Pages 使用文档站、安装与 Intel 排障指南。

## 1.0.0 — 2026-09-11

首次公开源码与双架构 macOS 发行包。

- 原生主面板、菜单栏和 76×76 圆形浮窗，深色/浅色主题。
- 稳定宿主和按需主面板进程，独立显示、隐藏和关闭。
- 主面板与浮窗各自支持时间、模型、任务筛选；柱状图悬停精确计数。
- 5 点额度圆弧、立即开始的展开/收起动画、左键/右键/拖动分工。
- 手动刷新反馈、可选自动刷新间隔、变化驱动增量索引。
- 账号额度和重置卡数量只读显示；没有兑换操作。
- 收录可选 Token 技能、Stop hook、合成测试和第三方许可证。
- 构建使用固定校验的离线运行组件，发行按 Apple Silicon / Intel 分包。

当前为 ad hoc 签名，未做 Apple 公证；实体 Intel 和其它 macOS 版本的验证边界见文档。
